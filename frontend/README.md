# Frontend

Nextjs app from template

## Notes

Using `.env.development` and `.env.production` because I do not want to deal with custom injecting and dealing with `.env`
So using what `nextjs` has built in already
